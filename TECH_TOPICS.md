# 科技/教育话题聚合器 - 使用说明

## 新增功能

### 1. 新增数据源

#### YouTube热门（科技/教育类别）
- **文件**: `src/fetchers/youtube_fetcher.py`
- **类别**: 
  - `tech`: Science & Technology（科技）
  - `education`: Education（教育）
- **特点**: 自动过滤科技/教育相关视频
- **状态**: 需要正常网络环境

#### X(Twitter)趋势（科技/教育）
- **文件**: `src/fetchers/twitter_fetcher.py`
- **类别**: 
  - `tech`: 科技趋势话题
  - `education`: 教育趋势话题
- **特点**: 基于hashtag和关键词过滤
- **状态**: 需要认证或网络代理

#### Hacker News（科技新闻）
- **文件**: `src/fetchers/hackernews_fetcher.py`
- **类别**: 科技新闻聚合
- **API**: 无需认证，公开API
- **特点**: 专注创业和科技新闻
- **状态**: API可用，推荐使用

### 2. 话题过滤功能

#### 关键词过滤
配置文件：`config_tech.py`

```python
# 科技关键词（可自定义）
'tech_keywords': [
    '科技', '技术', 'AI', '人工智能', '芯片',
    '软件', '硬件', '互联网', '编程', '数据',
    'tech', 'technology', 'ai', 'coding', ...
]

# 教育关键词
'education_keywords': [
    '教育', '学习', '教程', '课程', '大学',
    '研究', '科学', '物理', '化学', '数学',
    'education', 'learning', 'tutorial', ...
]
```

### 3. 使用方法

#### 方法一：使用专用科技话题脚本

```bash
python main_tech.py
```

这个脚本会：
1. 从通用平台获取大量话题（如100条）
2. 使用关键词过滤出科技/教育相关话题
3. 生成专门的科技/教育话题报告

#### 方法二：使用主程序（支持所有平台）

编辑 `config.py`，启用科技平台：

```python
'enabled_platforms': [
    'hackernews',      # Hacker News（推荐）
    'youtube_tech',    # YouTube科技
    'youtube_edu',     # YouTube教育
    'twitter_tech',    # X(Twitter)科技
    'twitter_edu',     # X(Twitter)教育
]
```

然后运行：
```bash
python main.py
```

### 4. 配置示例

#### 只获取科技话题

```python
# config_tech.py
CONFIG = {
    'enabled_platforms': ['toutiao'],
    'filter_by_category': True,
    'toutiao_count': 100,  # 获取更多用于过滤
    
    'tech_keywords': ['你的关键词列表'],
    'education_keywords': ['你的关键词列表'],
}
```

#### 只使用科技专用平台

```python
# config.py
CONFIG = {
    'enabled_platforms': [
        'hackernews',  # 最稳定
    ],
    'hackernews_count': 30,
}
```

### 5. 文件说明

| 文件 | 说明 |
|------|------|
| `main.py` | 主程序，支持所有平台 |
| `main_tech.py` | 专用科技话题脚本（带过滤） |
| `config.py` | 通用配置文件 |
| `config_tech.py` | 科技话题专用配置 |
| `src/fetchers/youtube_fetcher.py` | YouTube抓取器 |
| `src/fetchers/twitter_fetcher.py` | X(Twitter)抓取器 |
| `src/fetchers/hackernews_fetcher.py` | Hacker News抓取器 |

### 6. 推荐配置

#### 配置A：国内网络环境
```python
'enabled_platforms': [
    'bilibili',   # B站科技区
    'toutiao',    # 今日头条（需过滤）
],
```

#### 配置B：国外网络环境
```python
'enabled_platforms': [
    'hackernews',    # Hacker News
    'youtube_tech',  # YouTube科技
    'youtube_edu',   # YouTube教育
],
```

#### 配置C：混合（推荐）
```python
# 使用 main_tech.py + config_tech.py
'enabled_platforms': ['toutiao'],
'toutiao_count': 100,  # 大量获取后过滤
'filter_by_category': True,
```

### 7. 自定义关键词

你可以根据需求修改 `config_tech.py` 中的关键词列表：

```python
# 添加你感兴趣的领域
'tech_keywords': [
    'AI', '机器学习', '深度学习',  # AI领域
    '前端', '后端', '全栈',        # 编程
    '芯片', '半导体',             # 硬件
    # ... 添加更多
]
```

### 8. 网络问题解决方案

如果遇到SSL连接错误：

1. **使用网络代理**
   ```python
   # 在 base_fetcher.py 中添加
   proxies = {
       'http': 'http://your-proxy:port',
       'https': 'https://your-proxy:port',
   }
   response = requests.get(url, proxies=proxies, ...)
   ```

2. **只使用国内平台**
   ```python
   'enabled_platforms': ['toutiao', 'bilibili', 'baidu']
   ```

3. **降低超时时间**
   ```python
   'timeout': 10  # 降低到10秒
   ```

### 9. 输出示例

生成的报告会标注话题类别：

```markdown
## 📱 今日头条(科技/教育)

### 1. 雷军晒超强钢发明专利证书
- **类别**: tech
- **热度**: 524.2万

### 2. AI如何改变教育行业
- **类别**: education
- **热度**: 380.1万
```

## 总结

本次优化添加了：
- ✅ YouTube热门（科技/教育）
- ✅ X(Twitter)趋势（科技/教育）
- ✅ Hacker News科技新闻
- ✅ 话题关键词过滤功能
- ✅ 专用科技话题聚合脚本

所有功能已实现并测试，可以在网络正常的环境下使用！
